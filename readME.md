1.var can re-declared,let is re-assignable ,const is constant not able to change
2.it expands into individual elements
3.for each play action for each elements,map transform each elements,filter select elements by condition
4.arrow function has shorter syntax than function and use =>
5.template literals are way to create strings in JavaScript using backticks(``) instead of quotes